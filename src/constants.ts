// Keep in sync with `productName` in `package.json`
export const appName = 'Gmail Desktop'

// Keep in sync with `build.appId` in `package.json`
export const appId = 'io.cheung.gmail-desktop'

export const gmailUrl = 'https://mail.google.com/mail/u/0/'

export const googleAccountsUrl = 'https://accounts.google.com'

export const topElementHeight = 40 // Px

// Keep in sync with `repository` in `package.json`
export const gitHubRepoUrl = 'https://github.com/timche/gmail-desktop'

export const defaultAccountId = 'default'

export const mainWindowMinWidth = 780
